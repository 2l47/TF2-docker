#!/usr/bin/env python3

# Called by setup.py
def loader(profile_name, region_name, instance_number, container):
	print(f"This is an example module. Profile name {profile_name}, region {region_name}, instance number {instance_number}.")
