// Wait a frame before changing levels; fixes the Extended Map Configs plugin not loading configs for the first map
// Note that the global profile does NOT wait before changing levels; this file overwrites that one.
wait 1; changelevel koth_cascade
